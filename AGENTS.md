# Agent 开发指南

## 项目约定

- 包管理与脚本统一使用 `pnpm`；版本以 `package.json` 的 `packageManager` 字段为唯一来源，升级时同步更新该字段和锁文件。
- 修改前先阅读相关路由、服务函数和现有组件；不要为了未来需求新增抽象层、通用包装器或依赖。
- 新增或调整 Web 插件的入口、注册或依赖时，保持客户端导出、插件元数据、启动清单和依赖注入一致，并按改动范围验证启动、失败回滚与卸载时的资源清理。
- 需要运行时校验或共享契约的类型优先由 Zod Schema 推导；其余优先从函数签名和返回值推导，避免无依据的 `as`、多余泛型和重复类型。

## UI 与文案

- 优先复用 `@yunzhen/shadcn-ui` 中已有的 shadcn 组件；缺少基础组件时，确认生成目标为 `packages/shadcn-ui` 且不覆盖现有实现后，使用 shadcn CLI 添加，不要手写替代品或引入同类 UI 库。
- 图标使用已安装的 `lucide-react`；交互元素保留语义、键盘可用性和可访问名称。
- UI 文案面向用户，简洁说明结果和下一步；不暴露无助于用户决策的内部实现细节，错误提示提供必要上下文和可执行的下一步。
- 所有新增用户可见文案进入本地化资源，并同步维护全部支持语言。

## 测试与验证

- Vitest 单测与组件测试放在源码附近，命名为 `*.test.ts` 或 `*.test.tsx`；Playwright 测试放在 `e2e/`，命名为 `*.spec.ts`。
- 不要为框架行为、生成路由树或纯展示性静态标记堆砌测试。

## 工作流与 Git

- 修改前检查 `git status --short`，避免覆盖他人的暂存、未暂存或未跟踪改动。
- 提交前执行相关测试、`git diff --check`，并检查暂存区内容；提交使用简洁的 Conventional Commit。
- “提交代码”只创建本地提交；只有用户明确要求时才推送远端。
- 预提交钩子失败时先区分本次改动与既有问题；不要通过删除测试、放宽安全校验或提交密钥来让检查通过。
